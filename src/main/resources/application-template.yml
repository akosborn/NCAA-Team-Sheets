spring:
  application:
    name: ncaa_team_sheets
  datasource:
    url: jdbc:mysql://database-url:3306/database-name
    username: ***
    password: ***
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  thymeleaf:
    cache: true
